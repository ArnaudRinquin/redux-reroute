import React from 'react'; // eslint-disable-line no-unused-vars

export default ({myUrlParam}) => {
  return (
    <section>
      <h1>Page C - with url param: {myUrlParam}</h1>
    </section>
  );
};
